# Environment variables for Sentinel-2 Ship Chips pipeline

# AISStream API key (leave empty to use dry-run synthetic AIS)
AISSTREAM_API_KEY=your_aisstream_api_key_here

# Optional AWS credentials if writing to S3-compatible storage (not required for Earth Search reads)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
